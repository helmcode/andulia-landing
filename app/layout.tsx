import "./globals.css"
import type { Metadata } from "next"
import { Inter } from "next/font/google"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Andulia - AI-Powered Cloud Infrastructure Assistant",
  description:
    "Manage your cloud infrastructure effortlessly with natural language. Andulia helps you handle AWS resources and more.",
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
